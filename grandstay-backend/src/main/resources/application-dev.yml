spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

grandstay:
  security:
    jwt:
      secret: ${JWT_SECRET}

management:
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.grandstay: DEBUG
